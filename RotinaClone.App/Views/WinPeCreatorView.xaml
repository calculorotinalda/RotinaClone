<UserControl x:Class="RotinaClone.App.Views.WinPeCreatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vms="clr-namespace:RotinaClone.App.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vms:WinPeCreatorViewModel}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="1.2*" />
        </Grid.ColumnDefinitions>

        <!-- Media Build Selection Options (Left) -->
        <Grid Grid.Column="0" Margin="0,0,15,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Criar Mídia de Inicialização WinPE" Foreground="{DynamicResource TextPrimary}" FontSize="14" FontWeight="Bold" Margin="0,0,0,15" />

            <StackPanel Grid.Row="1">
                <Border Style="{DynamicResource GlassCard}" Padding="15" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Opção 1: Gravar em USB" Foreground="{DynamicResource TextPrimary}" FontSize="12" FontWeight="Bold" Margin="0,0,0,10" />
                        
                        <TextBlock Text="Selecione a unidade USB de destino:" Foreground="{DynamicResource TextSecondary}" FontSize="11" Margin="0,0,0,5" />
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ComboBox Grid.Column="0" ItemsSource="{Binding UsbDrives}" SelectedItem="{Binding SelectedUsbDrive}" Background="#182C48" BorderBrush="{DynamicResource CardBorder}" Foreground="{DynamicResource TextPrimary}" Padding="6" />
                            <Button Grid.Column="1" Content="⟳" Width="32" Height="30" Margin="5,0,0,0" Padding="0" Command="{Binding RefreshDrivesCommand}" ToolTip="Atualizar unidades" />
                        </Grid>

                        <Button Content="Gravar USB Bootável" Style="{DynamicResource AccentButton}" Command="{Binding CreateUsbCommand}" IsEnabled="{Binding IsReadyToBuild}" />
                    </StackPanel>
                </Border>

                <Border Style="{DynamicResource GlassCard}" Padding="15">
                    <StackPanel>
                        <TextBlock Text="Opção 2: Exportar ISO" Foreground="{DynamicResource TextPrimary}" FontSize="12" FontWeight="Bold" Margin="0,0,0,10" />

                        <TextBlock Text="Caminho do ficheiro ISO:" Foreground="{DynamicResource TextSecondary}" FontSize="11" Margin="0,0,0,5" />
                        <TextBox Text="{Binding IsoOutputPath}" Background="#10FFFFFF" BorderBrush="{DynamicResource CardBorder}" Foreground="{DynamicResource TextPrimary}" Padding="6" Margin="0,0,0,12" />

                        <Button Content="Gerar Imagem ISO" Command="{Binding CreateIsoCommand}" IsEnabled="{Binding IsReadyToBuild}" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <!-- Builder Progress and Logs (Right) -->
        <Border Grid.Column="1" Style="{DynamicResource GlassCard}" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Estado do Processo" Foreground="{DynamicResource TextPrimary}" FontSize="14" FontWeight="Bold" Margin="0,0,0,10" />
                
                <TextBlock Grid.Row="1" Text="{Binding StatusText, FallbackValue='Pronto'}" Foreground="{DynamicResource PrimaryAccent}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,5" />
                
                <ProgressBar Grid.Row="2" Height="14" Value="{Binding ProgressPercent}" Minimum="0" Maximum="100" Margin="0,0,0,15">
                    <ProgressBar.Foreground>
                        <SolidColorBrush Color="#00BFFF" />
                    </ProgressBar.Foreground>
                </ProgressBar>

                <Border Grid.Row="3" Background="#20000000" BorderBrush="{DynamicResource CardBorder}" BorderThickness="1" CornerRadius="6" Padding="10">
                    <TextBox Text="{Binding BuildLogs}" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextPrimary}" FontFamily="Consolas" FontSize="10" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextChanged="TextBox_TextChanged" />
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
