<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Fonts -->
    <FontFamily x:Key="MainFont">Segoe UI, Outfit, Inter</FontFamily>

    <!-- Core Palette -->
    <Color x:Key="WindowBgColor1">#081B33</Color>
    <Color x:Key="WindowBgColor2">#0D2C54</Color>
    <SolidColorBrush x:Key="WindowBackground" Color="#081B33" />
    <LinearGradientBrush x:Key="WindowBackgroundGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#081B33" Offset="0" />
        <GradientStop Color="#0D2C54" Offset="0.5" />
        <GradientStop Color="#124A7A" Offset="1" />
    </LinearGradientBrush>

    <!-- Glassmorphic Card Brushes -->
    <SolidColorBrush x:Key="CardBackground" Color="#182C48" />
    <SolidColorBrush x:Key="CardBorder" Color="#304870" />
    <SolidColorBrush x:Key="CardHeaderBackground" Color="#102038" />

    <!-- Interactive Colors -->
    <Color x:Key="PrimaryAccentColor">#FF8C00</Color>
    <Color x:Key="PrimaryAccentHoverColor">#FF6A00</Color>
    <Color x:Key="InfoColorValue">#00BFFF</Color>
    <Color x:Key="SuccessColorValue">#2E7D32</Color>
    <Color x:Key="ErrorColorValue">#C62828</Color>

    <!-- Interactive Brushes -->
    <SolidColorBrush x:Key="PrimaryAccent" Color="{DynamicResource PrimaryAccentColor}" /> <!-- Laranja Premium -->
    <SolidColorBrush x:Key="PrimaryAccentHover" Color="{DynamicResource PrimaryAccentHoverColor}" /> <!-- Laranja Destaque -->
    <SolidColorBrush x:Key="InfoColor" Color="{DynamicResource InfoColorValue}" /> <!-- Deep Sky Blue -->
    <SolidColorBrush x:Key="SuccessColor" Color="{DynamicResource SuccessColorValue}" /> <!-- Green -->
    <SolidColorBrush x:Key="ErrorColor" Color="{DynamicResource ErrorColorValue}" /> <!-- Red -->

    <!-- Text Brushes -->
    <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF" />
    <SolidColorBrush x:Key="TextSecondary" Color="#A0B2C6" />
    <SolidColorBrush x:Key="TextDark" Color="#1E2835" />

    <!-- Button Brushes -->
    <SolidColorBrush x:Key="ButtonBackground" Color="#1C3254" />
    <SolidColorBrush x:Key="ButtonBorder" Color="#FF8C00" />
    <SolidColorBrush x:Key="ButtonHover" Color="#FF8C00" />

    <!-- Sidebar Navigation Button Style -->
    <Style x:Key="SidebarButton" TargetType="RadioButton">
        <Setter Property="FontFamily" Value="{StaticResource MainFont}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Height" Value="45" />
        <Setter Property="Foreground" Value="{DynamicResource TextSecondary}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="GroupName" Value="Navigation" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid Background="{TemplateBinding Background}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="6" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border x:Name="ActiveIndicator" Grid.Column="0" Background="Transparent" CornerRadius="0,3,3,0" Width="4" HorizontalAlignment="Left" />
                        <ContentPresenter Grid.Column="1" VerticalAlignment="Center" Margin="15,0,0,0" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#10FFFFFF" />
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryAccent}" />
                            <Setter Property="Background" Value="#15FF8C00" />
                            <Setter TargetName="ActiveIndicator" Property="Background" Value="{DynamicResource PrimaryAccent}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Glassmorphic Border/Card Style -->
    <Style x:Key="GlassCard" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource CardBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CardBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="15" ShadowDepth="1" Opacity="0.15" Color="#000000" />
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Modern Premium Button Style -->
    <Style TargetType="Button">
        <Setter Property="FontFamily" Value="{StaticResource MainFont}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Padding" Value="15,10" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
        <Setter Property="Background" Value="{DynamicResource ButtonBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource PrimaryAccent}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryAccentHover}" />
                            <Setter Property="Foreground" Value="{DynamicResource TextDark}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Accent/Orange Button Style -->
    <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="{DynamicResource PrimaryAccent}" />
        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryAccentHover}" />
        <Setter Property="Foreground" Value="{DynamicResource TextDark}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource PrimaryAccentHover}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryAccent}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
