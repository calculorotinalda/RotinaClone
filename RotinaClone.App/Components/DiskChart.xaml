<UserControl x:Class="RotinaClone.App.Components.DiskChart"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignWidth="400" d:DesignHeight="150"
             Background="Transparent">
    <Grid>
        <!-- Background grid lines -->
        <Canvas x:Name="ChartGridCanvas">
            <!-- Drawn in code-behind based on size -->
        </Canvas>
        
        <!-- Graph lines and areas -->
        <Canvas x:Name="ChartCanvas">
            <!-- Read Speed Area (Gradient) -->
            <Path x:Name="ReadAreaPath" Opacity="0.15">
                <Path.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#00BFFF" Offset="0" />
                        <GradientStop Color="#00BFFF" Offset="1" />
                    </LinearGradientBrush>
                </Path.Fill>
            </Path>
            
            <!-- Read Speed Line -->
            <Path x:Name="ReadLinePath" Stroke="#00BFFF" StrokeThickness="2" StrokeEndLineCap="Round" StrokeStartLineCap="Round" />

            <!-- Write Speed Area (Gradient) -->
            <Path x:Name="WriteAreaPath" Opacity="0.15">
                <Path.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#FF8C00" Offset="0" />
                        <GradientStop Color="#FF8C00" Offset="1" />
                    </LinearGradientBrush>
                </Path.Fill>
            </Path>

            <!-- Write Speed Line -->
            <Path x:Name="WriteLinePath" Stroke="#FF8C00" StrokeThickness="2" StrokeEndLineCap="Round" StrokeStartLineCap="Round" />
        </Canvas>

        <!-- Speed labels -->
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Text="Velocidade em tempo real (MB/s)" Foreground="{DynamicResource TextSecondary}" FontSize="10" Grid.Row="0" />
            <StackPanel Grid.Row="0" HorizontalAlignment="Right" Orientation="Horizontal">
                <Ellipse Width="6" Height="6" Fill="#00BFFF" Margin="0,0,4,0" VerticalAlignment="Center" />
                <TextBlock Text="Leitura" Foreground="{DynamicResource TextSecondary}" FontSize="9" Margin="0,0,12,0" />
                <Ellipse Width="6" Height="6" Fill="#FF8C00" Margin="0,0,4,0" VerticalAlignment="Center" />
                <TextBlock Text="Escrita" Foreground="{DynamicResource TextSecondary}" FontSize="9" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
